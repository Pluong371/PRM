package com.shopweb.controller.staff;

import org.springframework.web.bind.annotation.*;

/**
 * Controller for staff inventory reporting
 */
@RestController
@RequestMapping("/api/staff/inventory")
public class StaffInventoryController {
    
    // POST /api/staff/inventory/reports - Create inventory report
    // GET /api/staff/inventory/reports - Get inventory reports
    // GET /api/staff/inventory/reports/{id} - Get report details
}
